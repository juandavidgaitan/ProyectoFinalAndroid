<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.veterinaria">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".BuscarCliente"></activity>
        <activity android:name=".MenuVeterinaria" />
        <activity android:name=".MenuCliente" />
        <activity android:name=".RegistrarVeterinario" />
        <activity android:name=".RegistrarServicios" />
        <activity android:name=".RegistrarCita" />
        <activity android:name=".RegistrarMascota" />
        <activity android:name=".RegistrarCliente" />
        <activity android:name=".RegistrarVeterinaria" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>